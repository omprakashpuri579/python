#exercise to find are of rectange
length = float(input("Enter the length of the rectange : "))
width = float(input("Enter the width of the rectange : "))

area_of_rectangle = length * width
print(f"The area of rectangle is {area_of_rectangle}")